#include <iostream>
#include <set>
using namespace std;
set <int> Set1, Set2;
void Init(int n)
{
    for(int i = 1; i <= n; ++i)
        Set1.insert(i);
    while(Set1.find())
}
int main()
{
    int m, n;
    cin >> m >> n;
    Init(n);
    return 0;
}
