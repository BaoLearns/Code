#include <stdio.h>
#include <stdlib.h>
#define max 100;
int a[max];
int b[max];
int main()
{
    int L,N,P;
    while(scanf("%d", &N))
    {
        int i;
        for(i=0;i<N;++i)
            scanf("%d%d",a[i],b[i]);
        scanf("%d%d",&L,&P);
        a[N]=b[N]=0;
        int d;
        while(d)
        {

        }
    }
    return 0;
}
